# PRD: 나만의 관심종목 대시보드

## 1. 개요
- **목적**: 출근 직후 5분 안에 내 관심종목, 메모, 오늘 할 일을 한 화면에서 훑어볼 수 있는 개인용 웹 대시보드.
- **배경**: 증권사 신입사원이 매일 아침 여러 곳(HTS, 메모앱, 수기 체크리스트)에 흩어진 정보를 확인하는 대신, 출근 후 가장 먼저 여는 한 화면으로 통합.
- **범위**: 이번 버전은 개인 브라우저에서 동작하는 정적 웹 페이지 1개. 서버/로그인 없음.

## 2. 사용자 & 시나리오
- **사용자**: 나 (증권사 신입사원), 하루 1회, 출근 직후 약 5분 사용.
- **시나리오**:
  1. 출근해서 브라우저로 대시보드를 연다.
  2. 상단에서 오늘 날짜와 인사말을 확인한다.
  3. 좌측 관심종목 카드를 훑어보며 오늘 챙겨볼 종목을 떠올린다. 필요하면 새 종목을 추가하거나 더 이상 안 볼 종목을 삭제한다.
  4. 우측 메모를 확인/추가하고, 오늘의 체크리스트를 확인하며 항목을 체크하거나 새로 추가한다.
  5. 5분 내로 화면을 닫고 하루 업무를 시작한다.

## 3. 핵심 기능 명세

### 3.1 관심종목 등록·삭제
- **입력**: 종목명(필수), 종목코드(선택)
- **동작**: 입력 후 등록 버튼 클릭 시 좌측에 카드로 추가됨. 카드마다 삭제 버튼이 있어 클릭 시 즉시 제거됨.
- **표시 정보**: 종목명, 종목코드(있으면). 실시간 시세는 이번 범위에서 제외되므로 가격/등락률은 표시하지 않음.
- **완료 기준**: 종목을 등록하면 카드가 나타나고, 삭제하면 사라진다. 새로고침 후에도 등록된 종목 목록이 유지된다.

### 3.2 메모 기록
- **형태**: 자유 텍스트 입력 영역 1개 (종목별 메모가 아닌, 하루 전체를 위한 공용 메모장).
- **동작**: 입력한 내용은 자동 저장되거나, 저장 버튼으로 저장.
- **완료 기준**: 메모를 입력하고 새로고침해도 내용이 남아있다.

### 3.3 오늘의 체크리스트
- **형태**: 항목 추가, 체크(완료 표시), 삭제가 가능한 할 일 목록.
- **동작**: 텍스트 입력 후 추가 버튼으로 항목 생성, 체크박스로 완료 표시, 항목별 삭제 버튼 제공.
- **완료 기준**: 항목을 추가·체크·삭제할 수 있고, 새로고침 후에도 상태가 유지된다.

## 4. 화면 구성

```
┌──────────────────────────────────────────────────┐
│  2026년 8월 12일 (수) · 좋은 아침입니다 👋           │  ← 상단: 날짜·인사
├───────────────────────┬────────────────────────────┤
│  관심종목               │  메모                        │
│  [+ 종목 추가 입력창]    │  [자유 텍스트 입력 영역]        │
│  ┌───────────────┐    │                              │
│  │ 삼성전자   [삭제] │    ├────────────────────────────┤
│  ├───────────────┤    │  오늘의 체크리스트              │
│  │ SK하이닉스 [삭제] │    │  [+ 항목 추가 입력창]          │
│  ├───────────────┤    │  ☐ 시황 브리핑 확인             │
│  │ ...           │    │  ☑ 관심종목 공시 확인           │
│  └───────────────┘    │  ...                          │
└───────────────────────┴────────────────────────────┘
   좌측: 관심종목 카드          우측: 메모 · 체크리스트
```

## 5. 제외 범위 (Out of Scope)
- 실시간 시세 연동 (가격/등락률 표시 없음, 종목명 등록만)
- 로그인/계정/사용자 구분 (단일 사용자, 인증 없음)
- 모바일 앱 (데스크톱 브라우저 기준. 모바일 웹 최적화도 이번 범위 아님)

## 6. 데이터 저장 방식 (가정)
- 서버 없이 브라우저 로컬 저장소(localStorage)에 관심종목/메모/체크리스트를 저장.
- 저장 위치가 브라우저 단위이므로, 다른 기기·다른 브라우저에서는 데이터가 보이지 않음(로그인 없는 범위이므로 허용되는 제약).
- 새로고침·재접속 시에도 직전 상태가 유지되어야 함(5분짜리 아침 루틴이 매일 이어지려면 필수).

## 7. 검증 기준 (완료 정의)
- 브라우저에서 아래 3가지 기능이 모두 정상 동작하면 완성으로 간주한다.
  1. 관심종목 등록·삭제
  2. 메모 기록(입력·저장)
  3. 오늘의 체크리스트(추가·체크·삭제)
- (권장 추가 기준) 새로고침 후에도 위 3가지 상태가 유지되는지 확인한다. — 저장 방식(6번)이 실제로 동작하는지 검증하는 항목이므로 함께 확인 권장.

## 8. 가정 및 확인 필요 사항
아래는 원 요구사항에 명시되지 않아 이번 PRD 작성 시 임의로 가정한 부분이다. 실제 구현 전에 맞는지 확인 필요:
- **체크리스트 초기화 여부**: 이번 버전은 "매일 자동 초기화" 없이 계속 누적되는 목록으로 가정(수동으로 지우기 전까지 유지). 매일 자정 자동 리셋이 필요하면 별도 요구사항으로 추가해야 함.
- **메모의 단위**: 종목별 메모가 아니라 하루 전체를 위한 공용 메모 1개로 가정. 종목별 메모가 필요하면 카드 구조를 변경해야 함.
- **인사말 문구**: 시간대(아침/오후)에 따라 달라지는 문구인지, 고정 문구인지는 미정. 우선 고정 인사말로 가정.
